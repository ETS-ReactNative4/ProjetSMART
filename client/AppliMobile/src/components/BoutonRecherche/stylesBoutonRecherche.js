import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  rechercheLieu: {
    paddingBottom: 50,
    paddingLeft: 30,
    paddingRight: 30,
    justifyContent: 'center',
    zIndex: 1
  }
});
